{
  "name": "limbos",
  "author": "liuyll",
  "license": "MIT",
  "main": "src",
  "repository": {
    "type": "git",
    "url": "https://github.com/liuyll/limbo"
  },
  "workspaces": [
    "src/limbo-template-parser"
  ],
  "scripts": {
    "build": "rollup -c",
    "build-all": "npm run build && npm run build --workspaces"
  },
  "files": [
    "lib/**/*.js",
    "lib/**/*.map",
    "src/**"
  ],
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "eslint": "^6.8.0",
    "rollup": "^2.32.1"
  },
  "dependencies": {
    "checkpromise": "^1.0.0",
    "shallowequal": "^1.1.0"
  }
}
